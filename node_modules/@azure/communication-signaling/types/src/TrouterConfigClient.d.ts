import * as coreClient from "@azure/core-client";
import { CommunicationTokenCredential, SignalingClientOptions } from "./SignalingClient";
export declare class TrouterConfigClient extends coreClient.ServiceClient {
    private endpoint;
    apiVersion: string;
    private httpClient;
    constructor(endpoint: string, options?: SignalingClientOptions);
    fetchServiceUrls(credential: CommunicationTokenCredential): Promise<RealTimeNotificationConfiguration>;
}
export declare class RealTimeNotificationConfiguration {
    trouterServiceUrl: string;
    registrarServiceUrl: string;
    cloudType: string;
    constructor(trouterServiceUrl: string, registrarServiceUrl: string, cloudType: string);
}
